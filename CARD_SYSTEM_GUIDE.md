# 卡牌系统使用指南

## 功能概述

卡牌系统已成功集成到游戏中。每个玩家在选择国家后，会获得专属的技能卡（锦囊卡）。

## 卡牌说明

### 蜀汉（SHU）
1. **威震华夏** - 当敌方部队处于河岸边时，使用此技能卡，进攻该部队时战斗判定向利于其的方向移动一列
2. **七擒七纵** - 使用此技能卡额外获得一个任意部署在蜀国地盘的无当飞军单位
3. **空城妙计** - 使用此技能卡可指定一个格子在此大回合内不能被敌方进攻

### 孙吴（WU）
1. **火烧连营** - 当敌方在某地块堆叠部队数量超过1时，使用此技能卡，进攻该地块上的部队时战斗判定向利于其的方向移动一列
2. **白衣渡江** - 指定某格上所有部队，本大回合该部队不受跨河惩罚，且roll点点数+1
3. **刮目相看** - 使用此技能卡额外获得一个任意部署在吴国地盘的解烦兵单位

### 曹魏（WEI）
1. **偷渡阴平** - 指定某格上所有部队，本大回合其行动力+2且无视山地地形影响（移动和战斗）

## 使用方法

### 1. 查看卡牌
- 游戏开始后，右侧卡牌栏会显示当前可用的技能卡
- 卡牌名称清晰显示，未使用的卡牌以白色背景展示

### 2. 选中卡牌
- **鼠标左键点击** 卡牌栏中的卡牌以选中它
- 选中的卡牌会显示**金色边框**，表示已选中
- 信息面板会显示 "已选中: [卡牌名称]"

### 3. 查看卡牌描述
- **将鼠标悬停** 在卡牌上时，会显示该卡牌的效果描述
- 可在屏幕左侧信息面板中查看完整的效果说明

### 4. 使用卡牌
- 选中要使用的卡牌后，**按下 Enter 键** 打出该卡牌
- 系统会显示 "已使用技能卡: [卡牌名称]" 的确认消息
- 该卡牌会从右侧卡牌栏中消失

### 5. 已使用的卡牌
- 每张卡牌在整个游戏过程中只能使用一次
- 使用过的卡牌不再显示在右侧卡牌栏中
- 游戏重新开始时，所有卡牌会重置为可用状态

## 文件结构

```
assets/
└── data/
    └── cards.json          # 卡牌定义文件

src/
├── game_objects/
│   └── card.py             # 卡牌系统实现
└── ui/
    └── info_panel.py       # CardPanel UI组件

settings.py                  # 包含卡牌文件配置
```

## 技术细节

### 卡牌数据结构
每张卡牌包含以下属性：
- `id`: 唯一标识符
- `name`: 卡牌名称
- `country`: 所属国家 (SHU, WU, WEI)
- `description`: 效果描述
- `category`: 卡牌类别 (offensive, defensive, summon, buff)

### 核心类

#### CardDefinition
- 不可变的卡牌定义对象
- 包含卡牌的所有静态属性

#### CardState
- 运行时卡牌状态
- 追踪卡牌是否已被使用

#### CardRepository
- 卡牌仓库，加载和管理所有卡牌定义
- 提供按国家查询卡牌的功能

#### CardManager
- 为指定国家管理卡牌状态
- 追踪可用和已使用的卡牌
- 负责卡牌的使用逻辑

#### CardPanel (UI)
- 游戏右侧的卡牌显示面板
- 处理卡牌的视觉呈现和交互
- 支持选中、悬停提示等交互功能

## 扩展建议

未来可以实现以下功能：

1. **卡牌效果系统** - 在`_play_selected_card`方法中实现具体的游戏效果
2. **卡牌动画** - 添加卡牌使用时的视觉效果
3. **卡牌历史** - 显示已使用过的卡牌记录
4. **卡牌评分** - 根据卡牌效果的强度进行评分

## 测试方法

运行测试脚本验证卡牌系统：
```bash
python test_cards.py
```

这将显示所有卡牌的加载、国家分类和使用状态的测试结果。
